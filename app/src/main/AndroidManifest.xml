<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aashnashroff.domino">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlayActivity" />
        <activity android:name=".AccelPlayActivity" />
        <activity android:name=".LightPlayActivity" />
        <activity android:name=".ChallengeActivity" />
        <activity android:name=".BuildActivity" />
        <activity
            android:name=".LightInfoActivity"
            android:label="@string/title_activity_light_info"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>